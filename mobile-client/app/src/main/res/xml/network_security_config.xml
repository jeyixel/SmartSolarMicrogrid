<?xml version="1.0" encoding="utf-8"?>
<!--
    Android blocks plain HTTP by default since API 28. The development backend
    runs on http://localhost:5127, reached from the emulator as 10.0.2.2, so
    cleartext is permitted for those hosts ONLY. Everything else still requires
    HTTPS, and this exception must be removed before any real deployment.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
